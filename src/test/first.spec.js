'use strict';

const assert = require('assert');

/* eslint-disable */
describe('Simple Math Test', () => {
  it('should return 2', () => {
    assert.equal(1 + 1, 2);
  });
  // it('should return 9', () => {
  //   assert.equal(3 * 2, 9);
  // });
});
